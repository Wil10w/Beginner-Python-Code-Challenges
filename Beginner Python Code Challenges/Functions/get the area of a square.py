
def find_area(side_length):
    result = test_side_length * test_side_length
    return result





test_side_length = 4
print("A square with side length", test_side_length, "has an area of", find_area(test_side_length))
